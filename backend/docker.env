# ===================================
# Docker Environment Configuration for StaffFind Backend
# ===================================

# ===================================
# Database Configuration
# ===================================
# Database Provider: "postgres" (default) or "supabase"
DB_PROVIDER=postgres

# Local PostgreSQL (Docker) Configuration
DB_HOST=postgres
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=stafind
DB_SSLMODE=disable

# ===================================
# Supabase Configuration (Alternative)
# ===================================
# To use Supabase instead of local PostgreSQL:
# 1. Set DB_PROVIDER=supabase
# 2. Comment out the PostgreSQL configuration above
# 3. Uncomment and configure the Supabase settings below
# 4. Update docker-compose.dev.yml to not start the postgres service

# DATABASE_URL=postgresql://postgres.xxxxxxxxxxxx:[YOUR-PASSWORD]@aws-0-us-east-1.pooler.supabase.com:6543/postgres
# DB_PROVIDER=supabase
# DB_SSLMODE=require
# DB_MAX_OPEN_CONNS=25
# DB_MAX_IDLE_CONNS=5
# DB_CONN_MAX_LIFETIME=300
# DB_CONN_MAX_IDLE_TIME=60
# SUPABASE_POOLER_MODE=transaction

# ===================================
# Server Configuration
# ===================================
PORT=8080
HOST=0.0.0.0

# ===================================
# Flyway Configuration
# ===================================
FLYWAY_LOCATIONS=./flyway_migrations

# ===================================
# Logging
# ===================================
LOG_LEVEL=info

# ===================================
# CORS Configuration
# ===================================
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001
CORS_ALLOWED_METHODS=GET,POST,PUT,DELETE,OPTIONS
CORS_ALLOWED_HEADERS=Origin,Content-Type,Accept,Authorization
